import { Signal } from "@segment/analytics-signals-runtime";
import { AnyAnalytics } from "../../types";
import { SignalSandbox } from "./sandbox";
export declare class SignalEventProcessor {
    analytics: AnyAnalytics;
    sandbox: SignalSandbox;
    constructor(analytics: AnyAnalytics, sandbox: SignalSandbox);
    process(signal: Signal, signals: Signal[]): Promise<void>;
    cleanup(): void | Promise<void>;
}
//# sourceMappingURL=processor.d.ts.map